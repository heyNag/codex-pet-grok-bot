## Local preview server

- Use `npm run preview` from the repository root for manual browser testing.
- Keep the preview server in the foreground and stop it with `Control-C` when testing is complete.
- Do not require contributors to install a global process manager. An agent may use an available supervisor for its own session, but the documented project workflow must remain portable.
